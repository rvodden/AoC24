export const config = {
  plugins: ['prettier-plugin-organize-imports'],
  tabWidth: 4,
  singleQuote: true,
  quoteProps: 'consistent',
  jsxSingleQuote: true,
  trailingComma: 'all',
  bracketLine: true,
  allowParens: 'always',
};
